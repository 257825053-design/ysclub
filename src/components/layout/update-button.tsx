/**
 * UpdateButton - 更新按钮组件
 *
 * 更新功能已彻底禁用，此组件永远不渲染任何内容。
 * 保留组件导出以兼容现有引用，避免编译错误。
 */
interface Props {
  className?: string
}

export const UpdateButton = (_props: Props) => {
  // 更新功能已禁用，永远返回 null
  return null
}
